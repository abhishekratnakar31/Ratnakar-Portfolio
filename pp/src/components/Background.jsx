const Background = () => {
    return (
        <div className="absolute inset-0 w-full h-full pointer-events-none z-0">
            {/* User will add background content here */}
        </div>
    );
};

export default Background;
